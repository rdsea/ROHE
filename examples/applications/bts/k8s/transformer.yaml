apiVersion: apps/v1
kind: Deployment
metadata:
  name: transformer-inference
  namespace: bts
  labels:
    app: transformer-inference
    component: inference
spec:
  replicas: 1
  selector:
    matchLabels:
      app: transformer-inference
  template:
    metadata:
      labels:
        app: transformer-inference
    spec:
      containers:
        - name: transformer
          image: rohe/bts-inference:latest
          ports:
            - containerPort: 8000
          env:
            - name: MODEL_SERVICE
              value: transformer_inference
            - name: ROHE_SERVICE_NAME
              value: transformer
            - name: ROHE_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: rohe-config
                  key: endpoint
                  optional: true
            - name: ROHE_EXPERIMENT_ID
              valueFrom:
                configMapKeyRef:
                  name: rohe-config
                  key: experiment_id
                  optional: true
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "2"
              memory: "2Gi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: transformer-inference
  namespace: bts
spec:
  selector:
    app: transformer-inference
  ports:
    - port: 8000
      targetPort: 8000
