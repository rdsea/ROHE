apiVersion: apps/v1
kind: Deployment
metadata:
  name: statistical-inference
  labels:
    app: statistical-inference
    component: inference
spec:
  replicas: 1
  selector:
    matchLabels:
      app: statistical-inference
  template:
    metadata:
      labels:
        app: statistical-inference
    spec:
      containers:
        - name: statistical
          image: rdsea/bts-statistical-inference:0.0.1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: ROHE_SERVICE_NAME
              value: statistical
            - name: ROHE_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: rohe-config
                  key: endpoint
                  optional: true
            - name: ROHE_EXPERIMENT_ID
              valueFrom:
                configMapKeyRef:
                  name: rohe-config
                  key: experiment_id
                  optional: true
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: statistical-inference
spec:
  selector:
    app: statistical-inference
  ports:
    - port: 8000
      targetPort: 8000
