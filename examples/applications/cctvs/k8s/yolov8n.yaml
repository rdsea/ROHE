apiVersion: apps/v1
kind: Deployment
metadata:
  name: yolov8n-inference
  namespace: cctvs
  labels:
    app: yolov8n-inference
    component: inference
spec:
  replicas: 1
  selector:
    matchLabels:
      app: yolov8n-inference
  template:
    metadata:
      labels:
        app: yolov8n-inference
    spec:
      containers:
        - name: yolov8n
          image: rohe/cctvs-inference:latest
          ports:
            - containerPort: 8000
          env:
            - name: MODEL_SERVICE
              value: yolov8n_inference
            - name: ROHE_SERVICE_NAME
              value: yolov8n
            - name: ROHE_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: rohe-config
                  key: endpoint
                  optional: true
            - name: ROHE_EXPERIMENT_ID
              valueFrom:
                configMapKeyRef:
                  name: rohe-config
                  key: experiment_id
                  optional: true
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: yolov8n-inference
  namespace: cctvs
spec:
  selector:
    app: yolov8n-inference
  ports:
    - port: 8000
      targetPort: 8000
