apiVersion: apps/v1
kind: Deployment
metadata:
  name: x3d-xs-inference
  labels:
    app: x3d-xs-inference
    component: inference
    modality: video
spec:
  replicas: 1
  selector:
    matchLabels:
      app: x3d-xs-inference
  template:
    metadata:
      labels:
        app: x3d-xs-inference
    spec:
      containers:
        - name: x3d-xs
          image: rdsea/smartbuilding-x3d-xs-inference:0.0.1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: ROHE_SERVICE_NAME
              value: x3d-xs
            - name: ROHE_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: rohe-config
                  key: endpoint
                  optional: true
            - name: ROHE_EXPERIMENT_ID
              valueFrom:
                configMapKeyRef:
                  name: rohe-config
                  key: experiment_id
                  optional: true
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "2"
              memory: "4Gi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: x3d-xs-inference
spec:
  selector:
    app: x3d-xs-inference
  ports:
    - port: 8000
      targetPort: 8000
