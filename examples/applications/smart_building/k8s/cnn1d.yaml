apiVersion: apps/v1
kind: Deployment
metadata:
  name: cnn1d-inference
  namespace: smart-building
  labels:
    app: cnn1d-inference
    component: inference
    modality: timeseries
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cnn1d-inference
  template:
    metadata:
      labels:
        app: cnn1d-inference
    spec:
      containers:
        - name: cnn1d
          image: rohe/smart-building-inference:latest
          ports:
            - containerPort: 8000
          env:
            - name: MODEL_SERVICE
              value: cnn1d_inference
            - name: ROHE_SERVICE_NAME
              value: cnn1d
            - name: ROHE_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: rohe-config
                  key: endpoint
                  optional: true
            - name: ROHE_EXPERIMENT_ID
              valueFrom:
                configMapKeyRef:
                  name: rohe-config
                  key: experiment_id
                  optional: true
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "2Gi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: cnn1d-inference
  namespace: smart-building
spec:
  selector:
    app: cnn1d-inference
  ports:
    - port: 8000
      targetPort: 8000
