apiVersion: apps/v1
kind: Deployment
metadata:
  name: mobilenet-inference
  namespace: object-classification
  labels:
    app: mobilenet-inference
    component: inference
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mobilenet-inference
  template:
    metadata:
      labels:
        app: mobilenet-inference
    spec:
      containers:
        - name: mobilenet
          image: rohe/obj-class-inference:latest
          ports:
            - containerPort: 8000
          env:
            - name: MODEL_SERVICE
              value: mobilenet_inference
            - name: ROHE_SERVICE_NAME
              value: mobilenet
            - name: ROHE_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: rohe-config
                  key: endpoint
                  optional: true
            - name: ROHE_EXPERIMENT_ID
              valueFrom:
                configMapKeyRef:
                  name: rohe-config
                  key: experiment_id
                  optional: true
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: mobilenet-inference
  namespace: object-classification
spec:
  selector:
    app: mobilenet-inference
  ports:
    - port: 8000
      targetPort: 8000
