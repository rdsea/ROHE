apiVersion: apps/v1
kind: Deployment
metadata:
  name: yolov5l-inference
  labels:
    app: yolov5l-inference
    component: inference
spec:
  replicas: 1
  selector:
    matchLabels:
      app: yolov5l-inference
  template:
    metadata:
      labels:
        app: yolov5l-inference
    spec:
      containers:
        - name: yolov5l
          image: rdsea/cctvs-yolov5l-inference:0.0.1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: ROHE_SERVICE_NAME
              value: yolov5l
            - name: ROHE_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: rohe-config
                  key: endpoint
                  optional: true
            - name: ROHE_EXPERIMENT_ID
              valueFrom:
                configMapKeyRef:
                  name: rohe-config
                  key: experiment_id
                  optional: true
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "2"
              memory: "4Gi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: yolov5l-inference
spec:
  selector:
    app: yolov5l-inference
  ports:
    - port: 8000
      targetPort: 8000
