apiVersion: apps/v1
kind: Deployment
metadata:
  name: resnet50-inference
  labels:
    app: resnet50-inference
    component: inference
spec:
  replicas: 1
  selector:
    matchLabels:
      app: resnet50-inference
  template:
    metadata:
      labels:
        app: resnet50-inference
    spec:
      containers:
        - name: resnet50
          image: rdsea/objclass-resnet50-inference:0.0.1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: ROHE_SERVICE_NAME
              value: resnet50
            - name: ROHE_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: rohe-config
                  key: endpoint
                  optional: true
            - name: ROHE_EXPERIMENT_ID
              valueFrom:
                configMapKeyRef:
                  name: rohe-config
                  key: experiment_id
                  optional: true
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "2"
              memory: "4Gi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: resnet50-inference
spec:
  selector:
    app: resnet50-inference
  ports:
    - port: 8000
      targetPort: 8000
